using Godot;
using System;

public partial class SignpostOverwrite : Node
{
    /*[Export] public string Title = "";
    [Export] public string Details = "";

    public override void _Ready()
    {
        foreach (Node _node in GetChildren())
        {
            if (_node is Signpost signpost)
            {
                signpost.Title = Title;
                signpost.Details = Details;
                break;
            }
        }
    }*/
}
